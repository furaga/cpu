# Makefile.in
CC = g++
CXX = g++
LD = g++
RM = rm -f
CFLAGS = -Wall -O2 -funroll-loops
CXXFLAGS = -Wall -O2 -funroll-loops
LDFLAGS = -Wall -O2 -lm

###################################
# Set Vars below
###################################
#TEST_TARGET = $(subst ../../compiler/test/,, $(subst .s,, $(shell ls ../../compiler/test/*.s)))
TEST_TARGET = matmul
RT_TARGET = contest
#RT_TARGET = $(subst .sld,, $(shell ls ../sld))
SIM_FLAGS =
MIN_RT = ../../compiler/test/min-rt/min-rt
###################################
###################################


.PHONY: all asm sim clean rt
all: $(TARGET)
clean:
	$(RM) $(TARGET) $(RM_TARGET) *.o asmlog simlog *.lst
	
